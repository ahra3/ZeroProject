// app.js

// Function to greet the user
function greetUser(name) {
    return `Hello, ${name}! Welcome to ZeroProject.`;
}

// Example usage
const userName = 'Alice';
console.log(greetUser(userName));